import React from 'react';
import {View,Text} from 'react-native';

const RentDetail = ({item})=>{
  return(
    <View>
      <Text>RentDetail {item.id}</Text>
    </View>
  )
}

export default RentDetail;